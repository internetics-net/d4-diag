# Module Dependencies

```mermaid
graph LR
    classDef modStyle fill:#FF8F00,stroke:#E65100,color:white,font-weight:bold;

    id_test_example_py["test_example.py"]:::modStyle
```

This diagram shows the dependency relationships between different modules in the D4-Diag project.
